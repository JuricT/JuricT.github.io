define('app/filter', [], function () {

  class Filter {

    constructor(options) {
      // this.data = options.data;
    }

  }

  return Filter;
});

try {
  module.exports = Filter;
} catch (e) {}