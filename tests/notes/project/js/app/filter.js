define(
  'app/filter',
  [],
  function () {

    class Filter {

        constructor(options) {
          this.elements. = options.
        }

      }

    return Filter;
  }
);


try {
  module.exports = Filter;
} catch (e) {}
